<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         https://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="ingredientFindAll" class="java.lang.String">
        <constructor-arg value="select id, name, type from ingredients" />
    </bean>

    <bean id="ingredientFindById" class="java.lang.String">
        <constructor-arg value="select id, name, type from ingredients where id = ?" />
    </bean>

    <bean id="ingredientInsert" class="java.lang.String">
        <constructor-arg value="insert into ingredients (
        id, name, type) values (?, ?, ?)" />
    </bean>

    <bean id="insertTacoOrder" class="java.lang.String">
        <constructor-arg value="INSERT INTO taco_order (
        delivery_name, delivery_street, delivery_city, delivery_state, delivery_zip, cc_number, cc_expiration, cc_cvv, placed_at)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)"/>
    </bean>

    <bean id="insertIngredientReference" class="java.lang.String">
        <constructor-arg value="INSERT INTO ingredient_reference (
        ingredient_id, taco_id)
        VALUES (?, ?)" />
    </bean>

    <bean id="insertTaco" class="java.lang.String">
        <constructor-arg value="INSERT INTO taco (name, taco_order_id, created_at)
        VALUES (?, ?, ?)" />
    </bean>

    <bean id="getTacoOrder" class="java.lang.String">
        <constructor-arg value="SELECT id FROM taco_order order by id desc limit 1" />
    </bean>
</beans>